spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/celalgundogdu/turkcell-camp-config-server
          default-label: main
      profile: dev

server:
  port: 8888